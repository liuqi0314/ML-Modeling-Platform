# ML建模平台 26.0529.15

发布日期：2026-05-29

发行包：`ML建模平台_26.0529.15.zip`

SHA256：`3DF816ED2B1E036B0AB3FE02D2F6EF48052F8A3F3F9621901E419C907EC8171B`

## 发行重点

- 完成发行前模型计算链路复核，保留训练集 / 测试集作为对外展示和导出的主要评估口径。
- 优化模型训练稳定性：小样本场景会自动调整内部验证折数，KNN 会自动限制邻居数，减少调参失败。
- 优化 XGBoost、LightGBM、CatBoost 可用性检查，缺少依赖时不再用其他模型伪装替代。
- 修正 GeoShapley 坐标处理逻辑：预处理后会强制将指定经纬度字段置于最后两列，避免 one-hot 分类列被误识别为空间坐标。
- GeoShapley 失败时会明确退回标准 SHAP，并在报告中记录实际解释方法。
- 将原“PDP”对外表述调整为更准确的“SHAP-PDP”，避免把基于 SHAP 值的特征效应曲线误称为经典 PDP。
- 修复后续生成 SHAP-PDP、2D SHAP-PDP、空间图和聚类结果后，完整结果包不会同步刷新的问题。
- 结果评估页面和导出文件不再展示内部验证指标，仅保留 Train / Test 指标，界面更简洁。

## 包含内容

- 便携 Python 3.11 运行时。
- 已配置虚拟环境和核心依赖。
- Streamlit 建模平台主程序。
- 内置示例数据和示例 SHP。
- 启动脚本：`双击启动_平台.bat`。
- 关闭脚本：`关闭平台.bat`。

## 使用方式

1. 解压 `ML建模平台_26.0529.15.zip`。
2. 进入 `ML建模平台` 文件夹。
3. 双击 `双击启动_平台.bat`。
4. 浏览器打开后访问平台并运行建模流程。

## 兼容性

- Windows 10 / 11 64 位。
- 无需单独安装 Python。
- 常规使用无需联网。

## 发行备注

- `.runtime` 已清空，只保留运行时目录结构。
- 当前版本保留 `records` 历史记录目录；如需发布空白演示包，可在打包前清理 `records`。
- 本版本适合作为 `26.0529.15` 发行归档版本。
